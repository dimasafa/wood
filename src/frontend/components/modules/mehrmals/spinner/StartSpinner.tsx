import React from 'react';
import './startSpinner.scss';

const StartSpinner: React.FC = () => {
	return (
		<div className="spinner">
			<span className="spinner_loader"></span>
		</div>

		);
};

export default StartSpinner;